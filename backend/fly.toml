# fly.toml file generated for project-and-team-task-management-platform-d8wnzq

app = "project-and-team-task-management-platform-d8wnzq"
primary_region = "sin"

[build]
  dockerfile = "Dockerfile"

[deploy]
  release_command = "npx prisma migrate deploy"

[http_service]
  internal_port = 8080
  force_https = true
  auto_stop_machines = true
  auto_start_machines = true
  min_machines_running = 0
  processes = ["app"]
